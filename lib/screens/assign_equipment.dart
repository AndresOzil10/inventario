import 'package:flutter/material.dart';

class AssignEquipment extends StatelessWidget {
  const AssignEquipment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}